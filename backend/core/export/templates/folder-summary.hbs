{{!-- backend/core/export/templates/folder-summary.hbs - Template résumé de dossier LUCIDE --}}
<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Résumé du dossier - {{folder.name}}</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }
        
        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            line-height: 1.6;
            color: #333;
            background-color: #f8fafc;
        }
        
        .container {
            max-width: 1000px;
            margin: 20px auto;
            background: white;
            box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
            border-radius: 8px;
            overflow: hidden;
        }
        
        .header {
            background: linear-gradient(135deg, #3b82f6, #1e40af);
            color: white;
            padding: 40px;
            text-align: center;
        }
        
        .header h1 {
            font-size: 2.8em;
            margin-bottom: 15px;
            text-shadow: 0 2px 4px rgba(0,0,0,0.3);
        }
        
        .header .subtitle {
            font-size: 1.3em;
            opacity: 0.9;
            margin-bottom: 20px;
        }
        
        .header .folder-info {
            display: flex;
            justify-content: center;
            gap: 30px;
            margin-top: 20px;
            flex-wrap: wrap;
        }
        
        .header-stat {
            text-align: center;
        }
        
        .header-stat-number {
            font-size: 1.8em;
            font-weight: bold;
            display: block;
        }
        
        .header-stat-label {
            font-size: 0.9em;
            opacity: 0.8;
            text-transform: uppercase;
            letter-spacing: 0.5px;
        }
        
        .content {
            padding: 40px;
        }
        
        .section {
            margin-bottom: 40px;
            padding-bottom: 30px;
            border-bottom: 1px solid #e5e7eb;
        }
        
        .section:last-child {
            border-bottom: none;
            margin-bottom: 0;
        }
        
        .section-title {
            font-size: 1.6em;
            color: #1f2937;
            margin-bottom: 20px;
            padding-bottom: 10px;
            border-bottom: 3px solid #3b82f6;
            display: flex;
            align-items: center;
            gap: 12px;
        }
        
        .overview-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
            gap: 20px;
            margin-bottom: 30px;
        }
        
        .overview-card {
            background: linear-gradient(135deg, #f8fafc, #e2e8f0);
            border: 1px solid #cbd5e1;
            border-radius: 10px;
            padding: 25px;
            text-align: center;
            transition: transform 0.2s ease;
        }
        
        .overview-card:hover {
            transform: translateY(-2px);
        }
        
        .overview-number {
            font-size: 2.2em;
            font-weight: bold;
            color: #3b82f6;
            margin-bottom: 8px;
        }
        
        .overview-label {
            color: #64748b;
            font-weight: 600;
            text-transform: uppercase;
            letter-spacing: 0.5px;
            font-size: 0.9em;
        }
        
        .entities-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
            gap: 20px;
        }
        
        .entity-type-card {
            background: #fff;
            border: 1px solid #e5e7eb;
            border-radius: 8px;
            padding: 20px;
            border-left: 4px solid var(--entity-color);
        }
        
        .entity-type-header {
            display: flex;
            align-items: center;
            gap: 10px;
            margin-bottom: 15px;
        }
        
        .entity-type-icon {
            font-size: 1.5em;
        }
        
        .entity-type-name {
            font-weight: 600;
            font-size: 1.1em;
        }
        
        .entity-type-count {
            margin-left: auto;
            background: var(--entity-color);
            color: white;
            padding: 4px 10px;
            border-radius: 15px;
            font-size: 0.9em;
            font-weight: 600;
        }
        
        .entity-list {
            list-style: none;
        }
        
        .entity-list li {
            padding: 8px 0;
            border-bottom: 1px solid #f3f4f6;
            display: flex;
            justify-content: space-between;
            align-items: center;
        }
        
        .entity-list li:last-child {
            border-bottom: none;
        }
        
        .entity-name {
            font-weight: 500;
        }
        
        .entity-connections {
            color: #6b7280;
            font-size: 0.85em;
        }
        
        .relationships-summary {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
            gap: 20px;
        }
        
        .relation-type-card {
            background: #f8fafc;
            border: 1px solid #e2e8f0;
            border-radius: 8px;
            padding: 20px;
        }
        
        .relation-type-header {
            display: flex;
            align-items: center;
            justify-content: space-between;
            margin-bottom: 15px;
        }
        
        .relation-type-name {
            font-weight: 600;
            color: #374151;
        }
        
        .relation-count {
            background: #3b82f6;
            color: white;
            padding: 4px 10px;
            border-radius: 12px;
            font-size: 0.85em;
            font-weight: 600;
        }
        
        .timeline {
            background: #f8fafc;
            border: 1px solid #e2e8f0;
            border-radius: 8px;
            padding: 25px;
        }
        
        .timeline-item {
            display: flex;
            align-items: center;
            gap: 15px;
            padding: 15px 0;
            border-bottom: 1px solid #e5e7eb;
        }
        
        .timeline-item:last-child {
            border-bottom: none;
        }
        
        .timeline-date {
            background: #3b82f6;
            color: white;
            padding: 8px 12px;
            border-radius: 6px;
            font-size: 0.85em;
            font-weight: 600;
            white-space: nowrap;
        }
        
        .timeline-content {
            flex: 1;
        }
        
        .timeline-title {
            font-weight: 600;
            margin-bottom: 4px;
        }
        
        .timeline-desc {
            color: #6b7280;
            font-size: 0.9em;
        }
        
        .no-data {
            text-align: center;
            color: #9ca3af;
            font-style: italic;
            padding: 30px;
            background: #f9fafb;
            border-radius: 8px;
        }
        
        .footer {
            background: #f8fafc;
            padding: 30px 40px;
            border-top: 1px solid #e5e7eb;
            color: #6b7280;
            font-size: 0.9em;
        }
        
        .footer-grid {
            display: grid;
            grid-template-columns: 1fr 1fr;
            gap: 30px;
        }
        
        .warning-box {
            background: #fef3c7;
            border: 1px solid #f59e0b;
            border-radius: 8px;
            padding: 20px;
            margin: 20px 0;
        }
        
        .warning-title {
            color: #92400e;
            font-weight: 600;
            margin-bottom: 10px;
        }
        
        .warning-content {
            color: #b45309;
        }
        
        @media print {
            body { background: white; }
            .container { box-shadow: none; margin: 0; }
            .overview-card:hover { transform: none; }
            .footer { page-break-inside: avoid; }
        }
        
        @media (max-width: 768px) {
            .container { margin: 10px; }
            .header { padding: 20px; }
            .content { padding: 20px; }
            .header .folder-info { gap: 15px; }
            .overview-grid { grid-template-columns: 1fr; }
            .entities-grid { grid-template-columns: 1fr; }
        }
    </style>
</head>
<body>
    <div class="container">
        <!-- En-tête -->
        <div class="header">
            <h1>{{folder.name}}</h1>
            <div class="subtitle">Dossier d'enquête OSINT</div>
            <div class="folder-info">
                <div class="header-stat">
                    <span class="header-stat-number">{{count entities}}</span>
                    <span class="header-stat-label">Entités</span>
                </div>
                <div class="header-stat">
                    <span class="header-stat-number">{{count relationships}}</span>
                    <span class="header-stat-label">Relations</span>
                </div>
                <div class="header-stat">
                    <span class="header-stat-number">{{#if folder.file_count}}{{folder.file_count}}{{else}}0{{/if}}</span>
                    <span class="header-stat-label">Fichiers</span>
                </div>
            </div>
        </div>

        <!-- Contenu principal -->
        <div class="content">
            <!-- Vue d'ensemble -->
            <div class="section">
                <h2 class="section-title">
                    📊 Vue d'ensemble
                </h2>
                
                {{#if folder.description}}
                <div style="background: #f0f9ff; border: 1px solid #0ea5e9; border-radius: 8px; padding: 20px; margin-bottom: 25px;">
                    <h3 style="color: #0c4a6e; margin-bottom: 10px;">Description du dossier</h3>
                    <p style="color: #075985; white-space: pre-wrap;">{{folder.description}}</p>
                </div>
                {{/if}}

                <div class="overview-grid">
                    <div class="overview-card">
                        <div class="overview-number">{{formatDate folder.created_at 'DD/MM/YYYY'}}</div>
                        <div class="overview-label">Date création</div>
                    </div>
                    <div class="overview-card">
                        <div class="overview-number">{{formatDate folder.updated_at 'DD/MM/YYYY'}}</div>
                        <div class="overview-label">Dernière activité</div>
                    </div>
                    <div class="overview-card">
                        <div class="overview-number">{{#if stats.unique_types}}{{stats.unique_types}}{{else}}0{{/if}}</div>
                        <div class="overview-label">Types d'entités</div>
                    </div>
                    <div class="overview-card">
                        <div class="overview-number">{{#if stats.avg_connections}}{{stats.avg_connections}}{{else}}0{{/if}}</div>
                        <div class="overview-label">Connexions moy.</div>
                    </div>
                </div>
            </div>

            <!-- Entités par type -->
            {{#if entities}}
            {{#if (count entities)}}
            <div class="section">
                <h2 class="section-title">
                    🏷️ Entités par type
                </h2>
                <div class="entities-grid">
                    {{#each entityTypeGroups}}
                    <div class="entity-type-card" style="--entity-color: {{this.color}};">
                        <div class="entity-type-header">
                            <span class="entity-type-icon">{{this.icon}}</span>
                            <span class="entity-type-name">{{this.name}}</span>
                            <span class="entity-type-count">{{count this.entities}}</span>
                        </div>
                        {{#if this.entities}}
                        <ul class="entity-list">
                            {{#each this.entities}}
                            <li>
                                <span class="entity-name">{{this.name}}</span>
                                <span class="entity-connections">{{this.connection_count}} connexions</span>
                            </li>
                            {{/each}}
                        </ul>
                        {{else}}
                        <div class="no-data" style="padding: 15px;">Aucune entité</div>
                        {{/if}}
                    </div>
                    {{/each}}
                </div>
            </div>
            {{else}}
            <div class="section">
                <h2 class="section-title">
                    🏷️ Entités
                </h2>
                <div class="no-data">
                    Aucune entité n'a été ajoutée à ce dossier
                </div>
            </div>
            {{/if}}
            {{/if}}

            <!-- Relations -->
            {{#if relationships}}
            {{#if (count relationships)}}
            <div class="section">
                <h2 class="section-title">
                    🔗 Analyse des relations
                </h2>
                <div class="relationships-summary">
                    {{#each relationshipStats}}
                    <div class="relation-type-card">
                        <div class="relation-type-header">
                            <span class="relation-type-name">{{this.type}}</span>
                            <span class="relation-count">{{this.count}}</span>
                        </div>
                        <div style="color: #6b7280; font-size: 0.9em;">
                            {{percentage this.count ../relationshipStats.length}}% du total
                        </div>
                    </div>
                    {{/each}}
                </div>
            </div>
            {{else}}
            <div class="section">
                <h2 class="section-title">
                    🔗 Relations
                </h2>
                <div class="no-data">
                    Aucune relation n'a été établie dans ce dossier
                </div>
            </div>
            {{/if}}
            {{/if}}

            <!-- Timeline des activités -->
            {{#if timeline}}
            {{#if (count timeline)}}
            <div class="section">
                <h2 class="section-title">
                    ⏱️ Chronologie des activités
                </h2>
                <div class="timeline">
                    {{#each timeline}}
                    <div class="timeline-item">
                        <div class="timeline-date">{{formatDate this.date 'DD/MM/YYYY'}}</div>
                        <div class="timeline-content">
                            <div class="timeline-title">{{this.title}}</div>
                            <div class="timeline-desc">{{this.description}}</div>
                        </div>
                    </div>
                    {{/each}}
                </div>
            </div>
            {{/if}}
            {{/if}}

            <!-- Entités centrales -->
            {{#if centralEntities}}
            {{#if (count centralEntities)}}
            <div class="section">
                <h2 class="section-title">
                    ⭐ Entités centrales
                </h2>
                <div style="background: #f0fdf4; border: 1px solid #22c55e; border-radius: 8px; padding: 20px;">
                    <p style="color: #15803d; margin-bottom: 15px; font-weight: 500;">
                        Les entités avec le plus de connexions dans le réseau :
                    </p>
                    <ul class="entity-list">
                        {{#each centralEntities}}
                        <li>
                            <span>
                                <span class="entity-type-icon">{{getEntityIcon this.type}}</span>
                                <strong>{{this.name}}</strong>
                                <span style="color: #6b7280; margin-left: 8px;">({{capitalize this.type}})</span>
                            </span>
                            <span style="background: #22c55e; color: white; padding: 2px 8px; border-radius: 10px; font-size: 0.8em;">
                                {{this.connection_count}} connexions
                            </span>
                        </li>
                        {{/each}}
                    </ul>
                </div>
            </div>
            {{/if}}
            {{/if}}

            <!-- Recommandations -->
            {{#if recommendations}}
            <div class="section">
                <h2 class="section-title">
                    💡 Recommandations d'investigation
                </h2>
                <div class="warning-box">
                    <div class="warning-title">Suggestions pour approfondir l'enquête :</div>
                    <div class="warning-content">
                        <ul style="margin-left: 20px; margin-top: 10px;">
                            {{#each recommendations}}
                            <li style="margin-bottom: 8px;">{{this}}</li>
                            {{/each}}
                        </ul>
                    </div>
                </div>
            </div>
            {{/if}}
        </div>

        <!-- Pied de page -->
        <div class="footer">
            <div class="footer-grid">
                <div>
                    <strong>LUCIDE - Application OSINT</strong><br>
                    Police Judiciaire<br>
                    Rapport généré le {{formatDate _meta.generatedAt 'DD/MM/YYYY HH:mm'}}<br>
                    <em>{{_meta.generator}}</em>
                </div>
                <div style="text-align: right;">
                    <strong>Classification:</strong> {{#if classification}}{{classification}}{{else}}Confidentiel{{/if}}<br>
                    <strong>Dossier ID:</strong> {{folder.id}}<br>
                    <strong>Version:</strong> {{_meta.version}}<br>
                    <strong>Total pages:</strong> 1
                </div>
            </div>
        </div>
    </div>
</body>
</html>